name: feed_module_example
description: "Example application for the feed_module package."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  feed_module:
    path: ../
  firebase_core: ^4.5.0
  flutter_riverpod: ^3.2.1
  google_fonts: ^8.0.1
  flutter_svg: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/